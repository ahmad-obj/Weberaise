// Node 22+ can execute the project's dependency-free TypeScript utility modules
// with --experimental-strip-types, so no generated test bundle is required.
console.log('No testable-module build step required on Node 22+.');
