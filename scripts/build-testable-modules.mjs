// Tests execute TypeScript source through the tsx loader, so no generated
// test bundle is required and the suite does not depend on Node's built-in
// TypeScript implementation.
console.log('No testable-module build step required; tests use tsx.');
